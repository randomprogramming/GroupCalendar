import React from 'react';
import {View} from 'react-native';
import Typography from '../../components/Typography';
import styles from './styles';

const LoginScreen = () => {
  return (
    <View>
      <Typography>hi login</Typography>
    </View>
  );
};

export default LoginScreen;
